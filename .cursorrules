# Sales BI Dashboard - Cursor Rules

## 프로젝트 개요
영업 BI 대시보드로, 고객 신뢰도 관리, 영업 파이프라인, MBM 타임라인을 시각화합니다.


## 📦 코드 구조 규칙

### 파일 구조
- **불필요한 re-export 금지**: 폴더 내에 단순히 `export * from './file'`만 하는 `index.ts` 파일을 만들지 마세요
  - ❌ 나쁜 예:
    ```typescript
    // index.ts
    export * from './queryHook';
    export * from './queryKey';
    ```
  - ✅ 좋은 예: 직접 import 사용
    ```typescript
    import { useGetCustomerSummary } from '@/repository/query/customerDetailApiController/queryHook';
    ```

### OpenAPI 자동 생성 타입 사용 주의
- **`ProductType`, `CompanySize` 타입은 `@/types/common`에서 import**
  - OpenAPI Generator가 한글/특수문자를 잘못 처리하여 `src/repository/openapi/model/product-type.ts`, `company-size.ts`에 오류 발생
  - 대신 `src/types/common.ts`에 정의된 타입 사용
  
  ❌ 나쁜 예:
  ```typescript
  import { ProductType, CompanySize } from '@/repository/openapi/model';
  ```
  
  ✅ 좋은 예:
  ```typescript
  import { ProductType, CompanySize } from '@/types/common';
  ```

- **OpenAPI 생성 파일 제외 설정**:
  - `src/repository/openapi/**` 디렉토리는 TypeScript, ESLint 검사에서 제외됨
  - `tsconfig.json`의 `exclude`에 설정됨
  - `eslint.config.js`의 `ignores`에 설정됨

---

## 🎨 스타일 규칙

### SCSS 작성 순서
1. 내용: after, before, content
2. 넘침: overflow
3. 위치: position, top, right, bottom, left
4. 흐름: float
5. 표시: display, flex 관련 속성
6. 크기: width, height, max-width, min-width
7. 간격: margin, padding
8. 테두리: border, border-radius
9. 배경: background
10. 그림자: box-shadow
11. 폰트: @include FontStyle(), letter-spacing, text-align
12. 동작: animation, transform, transition
13. 기타: visibility, opacity, cursor, z-index (z-index는 항상 마지막)

### 컴포넌트 사용
- Text, Badge, Button 등은 `src/components/common/atoms/` 활용
- SCSS에서 직접 font-size, color, border-radius 사용 지양

---

## 📝 응답 언어
- 항상 한국어로 응답
